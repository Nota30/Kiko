CREATE TABLE IF NOT EXISTS ROLEPLAY_COUNTS (id BIGSERIAL PRIMARY KEY, discord_id VARCHAR, type VARCHAR, sent INT DEFAULT 0, received INT DEFAULT 0, created_at TIMESTAMP,  updated_at TIMESTAMP DEFAULT NOW(), unique(discord_id, type));